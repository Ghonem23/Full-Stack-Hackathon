/**
 * TODO: replace MODEL_NAME once the team agrees on it — changing it here
 * updates the logo on both the brand panel and the mobile header.
 * The browser tab title lives in index.html and needs the same edit.
 */
export const MODEL_NAME = 'Model Name'
export const API_BASE_URL = 'http://localhost:5000/api'